{
  "name": "prestataires-api",
  "version": "1.0.0",
  "description": "API pour application de prestataires (magasins, restaurants, hôtels)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "init": "node scripts/init-database.js",
    "seed": "node scripts/seed-data.js",
    
    "test": "NODE_ENV=test jest --runInBand --setupFilesAfterEnv=./__tests__/setup.js",
    "test:watch": "NODE_ENV=test jest --watch --runInBand --setupFilesAfterEnv=./__tests__/setup.js",
    "test:coverage": "NODE_ENV=test jest --coverage --runInBand --setupFilesAfterEnv=./__tests__/setup.js"
  },
  "keywords": [
    "api",
    "prestataires",
    "sequelize",
    "nodejs",
    "express"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "mysql2": "^3.6.5",
    "sequelize": "^6.35.2",
    "express-validator": "^7.0.1",
    "multer": "^1.4.5-lts.1",
    "compression": "^1.7.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}
